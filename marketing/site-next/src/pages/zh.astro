---
import Marketing from '@layouts/Marketing.astro';
import { ctaPrimary, ctaNote, hasPrimaryCta, RELEASE } from '../release.mjs';

// Raw markup, ported from the redesign mockup (home.linked.html) the same way
// index.astro was: hero, works, steps, voice, ledger, price — section by
// section, using the new moshpit.css component classes (.kicker/.k, .chips,
// .panes, .voice/.overlay, table.ledger, .pricecard) instead of the retired
// .shell/.band/.eyebrow/.grid/.pricebox ones.
//
// Copy is carried over from THIS page's own prior Chinese wherever the
// content already existed (hero, three steps, the six privacy facts) rather
// than re-translated from the English mock. Two pieces are genuinely new
// this pass and got fresh Chinese instead of a literal translation of the
// English mock: the dedicated voice section (previously a bullet list buried
// in a feature row that no longer exists on this page) and the honest-ledger
// table (previously a 6-cell grid; the $6.99 cell is dropped here since the
// price section below already covers it).
//   - the hero's CTA row and price note stay driven by release.mjs, not
//     hard-coded copy
//   - every anchor points at a real /zh/... path, never "#"
//   - the price card reads RELEASE.price rather than repeating the literal
//     figure a second time in this file
const body = `<!-- ══ HERO ══ -->
<header class="hero" id="s-home">
  <div class="wrap">
    <div>
      <div class="kicker">在锁屏上回 agent</div>
      <h1>让你的 agent<br><span class="reach">找得到你</span><span class="cursor" aria-hidden="true"></span></h1>
      <p class="lede">Claude Code 在你的服务器上卡在一个权限询问。你的手机亮了。
      你在锁屏上点一下 <b>Allow</b>，它就继续跑下去——不用解锁、不用 SSH、
      不用走回工位。</p>
      <div class="cta-row">
        ${ctaPrimary('zh')}
        <a class="cta ${hasPrimaryCta ? 'ghost' : ''}" href="/zh/guide/claude-code">完整跑一遍</a>
        <a class="ghost" href="/zh/compare">和别的比怎么样</a>
      </div>
      <p class="pricenote">${ctaNote('zh')}</p>
    </div>

    <div class="lockmock" role="img" aria-label="iPhone 锁屏：一个 agent 在等你，不用解锁就能点 Allow 或 Deny">
      <div class="needs"><span class="dot"></span> 1 NEEDS YOU</div>
      <div class="lockdate" id="lockdate">Sat Jan 1</div>
      <div class="locktime" id="locktime">9:41</div>
      <div class="agentcard">
        <div class="row">
          <span class="who">Claude Code <span class="st">· 等你</span></span>
          <span class="for">6h 50m</span>
        </div>
        <div class="where">mac-studio · 1:claude</div>
        <div class="pills">
          <span class="pill allow">✓ Allow</span>
          <span class="pill deny">✕ Deny</span>
        </div>
        <div class="replies"><span class="reply">yes</span><span class="reply">continue</span></div>
        <div class="second">
          <span class="who">codex <span class="ok">· 干活中</span></span>
          <span class="for">2h 04m</span>
        </div>
      </div>
    </div>
  </div>
</header>

<!-- ══ WORKS WITH ══ -->
<section class="works" id="s-agents">
  <div class="wrap">
    <div class="k">支持哪些 agent</div>
    <div class="chips">
      <span class="chip"><i></i>Claude Code</span>
      <span class="chip"><i></i>Codex</span>
      <span class="chip"><i></i>opencode</span>
      <span class="chip"><i></i>aider</span>
      <span class="chip"><i></i>Gemini CLI</span>
      <span class="chip any">……任何跑在终端里的</span>
    </div>
    <p>没有什么"集成列表"要挤进去。Moshpit 驱动的是真正的 shell，
    所以它没听说过的 agent，在发布当天就能用。</p>
  </div>
</section>

<!-- ══ THREE STEPS ══ -->
<section class="steps" id="s-flow">
  <div class="wrap">
    <h2>三步，之后它就只是你的终端。</h2>
    <div class="panes">
      <article class="pane">
        <div class="title"><b>1</b> 添加主机</div>
        <div class="body">填 SSH 信息和密钥——也可以在手机上直接生成。选 tmux、herdr
        或都不用。首次连接会把主机指纹给你核对。</div>
      </article>
      <article class="pane">
        <div class="title"><b>2</b> 起 agent</div>
        <div class="body">在窗格里起 <code>claude</code>，或者让 Moshpit 开一个隔离的
        <code>git worktree</code>，把第一句话交给它。主机上没有新东西要学。</div>
      </article>
      <article class="pane">
        <div class="title"><b>3</b> 放下手机</div>
        <div class="body">当 agent 需要人的时候，你的锁屏会告诉你——连回答用的按钮
        一起给你。</div>
      </article>
    </div>
  </div>
</section>

<!-- ══ VOICE ══ -->
<section class="voice" id="s-voice">
  <div class="wrap">
    <div>
      <div class="k">语音 · 就在这台设备上</div>
      <h2>把 prompt 说出来，看清了再发。</h2>
      <p>终端的快捷条上有一个 mic 键。你说的话先变成文字，落在键盘上方那块预览面板里
      ——<b>按 Insert 才会真的敲进 shell</b>。转写完全在这台设备上进行，音频不会离开手机。
      开启本地 Whisper 模型后支持约 100 种语言，<b>一句话里中英夹杂也能听准</b>。</p>
    </div>
    <div class="overlay" role="img" aria-label="语音输入面板：正在听写、实时转写文字，以及 Cancel、Insert 两个按钮">
      <div class="head">
        <span class="rec"></span><span class="st">LISTENING</span>
        <span class="bars"><i></i><i></i><i></i><i></i><i></i></span>
      </div>
      <p class="txt">tail 一下部署日志<span class="ghosttxt">，只看失败的那些</span></p>
      <div class="acts"><span class="cancel">Cancel</span><span class="insert">⇥ Insert</span></div>
    </div>
  </div>
</section>

<!-- ══ HONEST LEDGER ══ -->
<section class="ledger" id="s-honest">
  <div class="wrap">
    <div class="k">诚实是结构决定的</div>
    <div class="tbl-wrap">
      <table class="ledger">
        <thead><tr><th>事项</th><th class="us">答案</th><th>原因</th></tr></thead>
        <tbody>
          <tr><td class="cap-col">要注册账号</td><td class="us yes">不用</td>
              <td>没有账号可注册，也就没有账号能泄露。</td></tr>
          <tr><td class="cap-col">埋点与统计</td><td class="us yes">零</td>
              <td>隐私标签上写着 "Data Not Collected"，因为确实什么都没收集。</td></tr>
          <tr><td class="cap-col">你的流量</td><td class="us yes">只到你自己的服务器</td>
              <td>没有中转、没有云端——我们根本不运营服务器。</td></tr>
          <tr><td class="cap-col">语音数据</td><td class="us yes">不出这台手机</td>
              <td>由 Apple 的语音模型或本地 Whisper 在本机转写，没有一条通向服务器的路。</td></tr>
          <tr><td class="cap-col">SSH 密钥</td><td class="us yes">留在 Secure Enclave</td>
              <td>由手机硬件生成并保管，用 Face ID 解锁。</td></tr>
        </tbody>
      </table>
    </div>
  </div>
</section>

<!-- ══ PRICE ══ -->
<section class="price" id="s-price">
  <div class="wrap">
    <div class="pricecard" style="margin:0 auto;text-align:left">
      <div><span class="amount">${RELEASE.price}</span><span class="once">一次性</span></div>
      <p class="pricefine" style="margin-top:14px">没有订阅、没有内购。
      价格会随 App 成熟往上调，早买就是折扣。</p>
    </div>
  </div>
</section>`;
---

<Marketing
  title="Moshpit — 让你的 agent 找得到你"
  description="为 AI coding agent 打造的 iOS 终端。锁屏上批准 Claude Code、一眼看清每个 agent 在等谁、手机上直接开隔离任务。SSH · Mosh · tmux · herdr。一次性买断，零数据收集。"
  path=""
  lang="zh"
  scrollspy={[
    { id: 's-home', label: '首页' },
    { id: 's-agents', label: '支持' },
    { id: 's-flow', label: '流程' },
    { id: 's-voice', label: '语音' },
    { id: 's-honest', label: '诚实' },
    { id: 's-price', label: '价格' },
  ]}
>
  <Fragment set:html={body} />

  <script>
    // The lock screen keeps real time — the one ambient detail that makes the
    // hero mock feel alive. Frozen under prefers-reduced-motion, same as every
    // other animation on this page (see moshpit.css's own blanket rule).
    if (!matchMedia('(prefers-reduced-motion: reduce)').matches) {
      const t = document.getElementById('locktime');
      const d = document.getElementById('lockdate');
      const tick = () => {
        const now = new Date();
        if (t) {
          t.textContent = now
            .toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' })
            .replace(/\s?[AP]M/i, '');
        }
        if (d) {
          d.textContent = now.toLocaleDateString([], { weekday: 'short', month: 'short', day: 'numeric' });
        }
      };
      tick();
      setInterval(tick, 15000);
    }
  </script>
</Marketing>
